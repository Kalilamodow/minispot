{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug minispot",
            "type": "cppvsdbg",
            "preLaunchTask": "Build minispot",
            "cwd": "${workspaceFolder}",
            "request": "launch",
            "program": "${workspaceFolder}/build/minispot.exe"
        }
    ]
}
